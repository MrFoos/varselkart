[pytest]
# Uten dette finner ikke `pytest` pakken `app` — pytest legger bare
# testmappa på sys.path, ikke backend-rota. (`python -m pytest` virker
# fordi -m legger til arbeidskatalogen, men CI kaller pytest direkte.)
pythonpath = .
testpaths = tests
